import { Typography } from "@mui/material";
import LayoutStep from "./LayoutStep";

const PhoneStep = () => {
  return (
    <LayoutStep>
      <Typography> Шаг с вводом телефона </Typography>
    </LayoutStep>
  );
};

export default PhoneStep;
